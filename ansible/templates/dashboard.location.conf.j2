location /dashboard {
  auth_basic "DCIC DataCave/DARRA Administrative Dashboard";
  auth_basic_user_file passwords;
  include proxy_params;
  proxy_pass http://unix:{{ virtualenv_path }}/dashboard.sock;
}

location /dashboard/static {
  autoindex on;
  alias {{ project_path }}/static/;
}
